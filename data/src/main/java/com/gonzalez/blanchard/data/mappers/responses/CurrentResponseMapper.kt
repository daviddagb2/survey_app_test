package com.gonzalez.blanchard.data.mappers.responses

/*
fun CurrentWeatherResponse.toCurrentBO(): CurrentBO {
    return CurrentBO(
        observationTime = this.observationTime ?: "",
        temperature = this.temperature ?: 0,
        weatherCode = this.weatherCode ?: 0,
        weatherIcons = this.weatherIcons ?: emptyList(),
        weatherDescriptions = this.weatherDescriptions ?: emptyList(),
        windSpeed = this.windSpeed ?: 0,
        windDegree = this.windDegree ?: 0,
        windDir = this.windDirection ?: "",
        pressure = this.pressure ?: 0,
        precip = this.precipitation ?: 0.0,
        humidity = this.humidity ?: 0,
        cloudcover = this.cloudCover ?: 0,
        feelslike = this.feelsLike ?: 0,
        uvIndex = this.uvIndex ?: 0,
        visibility = this.visibility ?: 0,
        isDay = this.isDay ?: "",
    )
}*/
